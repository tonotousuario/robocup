name: Publicar imágenes Docker (ghcr.io)

# Construye y publica las imágenes de producción en GitHub Container Registry.
#   - ghcr.io/<owner>/robocup-app  (target "app": PHP-FPM + Laravel)
#   - ghcr.io/<owner>/robocup-web  (target "web": nginx + estáticos)
#
# Se dispara al hacer push a main, al crear un tag vX.Y.Z, o manualmente.
# Usa el GITHUB_TOKEN integrado (no necesitas crear un PAT).

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # necesario para publicar en ghcr con GITHUB_TOKEN
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: app
            suffix: app
          - target: web
            suffix: web
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Login en ghcr.io
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Metadata (tags y labels)
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}-${{ matrix.suffix }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag
            type=sha,format=short

      - name: Build y push (${{ matrix.target }})
        uses: docker/build-push-action@v7
        with:
          context: .
          target: ${{ matrix.target }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
